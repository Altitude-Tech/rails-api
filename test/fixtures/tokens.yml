# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  id: 1
  token: <%= SecureRandom.hex %>
  expires: <%= (Time.now.utc + 1.day).to_s(:db) %>
  enabled: true

two:
  id: 2
  token: <%= SecureRandom.hex %>
  expires: <%= (Time.now.utc - 1.day).to_s(:db) %>
  enabled: true
